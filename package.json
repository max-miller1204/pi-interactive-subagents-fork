{
  "name": "pi-interactive-subagents",
  "version": "3.7.2",
  "description": "Interactive subagents for pi - spawn, orchestrate, and manage sub-agent sessions in tmux",
  "keywords": [
    "pi-package"
  ],
  "license": "MIT",
  "author": "Amos Blomqvist",
  "repository": {
    "type": "git",
    "url": "https://github.com/amosblomqvist/pi-interactive-subagents"
  },
  "type": "module",
  "scripts": {
    "check:pi-versions": "node scripts/check-pi-versions.mjs",
    "test": "npm run check:pi-versions && node scripts/run-tests.mjs --test test/test.ts",
    "test:integration": "npm run check:pi-versions && node scripts/run-tests.mjs --test --test-concurrency=1 test/integration/*.test.ts"
  },
  "pi": {
    "extensions": [
      "./pi-extension/subagents/index.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "0.87.0",
    "@earendil-works/pi-server": "0.87.0",
    "@earendil-works/pi-tui": "0.87.0",
    "typebox": "1.3.26"
  }
}
